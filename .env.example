# ISEYC Digital Operations Centre — environment variables
# Copy this file to .env and fill in real values. Never commit .env itself.

# --- Database (Postgres, Phase 3 — hosted on Supabase) ---------------------
DATABASE_URL=

# --- Supabase Auth (Phase 2) -------------------------------------------------
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
SUPABASE_JWT_SECRET=
OWNER_AUTH_USER_ID=

# --- Legacy Manus Forge config (cron-callback verification only) -----------
VITE_APP_ID=
JWT_SECRET=
OAUTH_SERVER_URL=
BUILT_IN_FORGE_API_URL=
BUILT_IN_FORGE_API_KEY=

# --- Notion secondary archive (optional, keeps Supabase lean) ---------------
# Internal integration token with insert access to the DOC Secondary Archive DB.
NOTION_TOKEN=
# Database id for "DOC Secondary Archive — Development Journey"
NOTION_JOURNEY_DATABASE_ID=eb01c8e79b0d4039b4c8312daa5ed892
